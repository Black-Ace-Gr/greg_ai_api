Django>=5.2,<6.2
djangorestframework>=3.15
django-filter>=24.0
django-cors-headers>=4.4
Pillow>=10.4

# Add when you wire up async generation (build plan step 6):
# celery>=5.4
# redis>=5.0

# Add when you wire up the chosen image/voice providers (build plan steps 4-5)
# and PostgreSQL for anything beyond local dev.

# Async execution (now wired in - see videostudio/celery.py, episodes/tasks.py)
celery>=5.4
redis>=5.0

# Inference server dependencies live in inference_server/requirements.txt
# (installed separately, on the GPU worker machine)

djangorestframework-simplejwt
dj-database-url
django-cors-headers